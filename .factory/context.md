# Context

- No `CONTEXT.md`, `AGENTS.md`, or `docs/adr/` files were present in this checkout.
- Repo: `seraph1nia/agent-plugins` is an APM marketplace; root README documents the marketplace and currently includes a short intro poem at the top.
- Relevant files: `README.md` (target for this task), `apm.yml` and `plugins/factory/apm.yml` (leave unchanged for this docs-only update).
- Validation guidance from `README.md` / `VALIDATION.md`: marketplace artifacts are generated under `.claude-plugin/marketplace.json`; avoid touching generated output unless regeneration is intended.
