# Context

- No `CONTEXT.md`, `AGENTS.md`, or `docs/adr/` were present in this repo snapshot, so there is no additional project memory to inherit.
- Relevant repo docs: [README.md](/workspace/repo/README.md) defines the marketplace README layout and top-level heading style.
- Validation convention: [scripts/validate.sh](/workspace/repo/scripts/validate.sh) and [.github/workflows/validation.yml](/workspace/repo/.github/workflows/validation.yml) cover manifest/artifact checks, but this work is README-only and should not require generated output changes.
