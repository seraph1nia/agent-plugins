# Project Bearings

- No `CONTEXT.md`, `AGENTS.md`, or `docs/adr/` files were present in this checkout.
- User-facing docs live in [`README.md`](/workspace/repo/README.md); the repo is a small APM marketplace with `apm.yml` plus `plugins/factory/apm.yml`.
- Relevant validation scripts are [`scripts/validate.sh`](/workspace/repo/scripts/validate.sh) and its APM/Claude helpers, but this task should not require manifest or generated-artifact changes.
- Keep the change scoped to the README unless a formatting issue makes a tiny structural tweak necessary.
