# Context for wf-8780744a

- No `CONTEXT.md`, `AGENTS.md`, or `docs/adr/` files are present in this repo snapshot.
- README is the only relevant project memory for this task; it currently starts with a 4-line poem before the marketplace/package docs (`README.md`).
- Keep the change scoped to the README intro text; preserve the existing markdown structure and line count at the top.
- Repo validation/docs note: marketplace artifact handling is documented in `README.md` and `VALIDATION.md`, but this task should not touch generated artifacts (`.claude-plugin/marketplace.json`).