# Plan

1. Update the opening poem in `README.md` so it’s about dogs, while preserving the rest of the README structure and marketplace docs.
2. Keep the change limited to documentation; do not modify `apm.yml`, `plugins/factory/apm.yml`, or generated marketplace artifacts.
3. Do a quick markdown sanity check after editing (headings, code fences, table formatting still intact).
