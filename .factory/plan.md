# Plan

1. Update `README.md` to include a 3-line poem in a clear, lightweight spot in the document, preserving the current marketplace overview and install instructions.
2. Keep the change scoped to documentation only; do not modify `apm.yml`, `plugins/factory/apm.yml`, or `.claude-plugin/marketplace.json`.
3. Do a final markdown spot-check to confirm the poem is exactly 3 lines and the README still reads cleanly with the existing sections.
