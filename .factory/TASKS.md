# Tasks: ws-062906dc — Add CONTRIBUTING.md

- [x] 1. Create `CONTRIBUTING.md` at the repo root with a one-paragraph overview covering what the repo is, its structure (`apm.yml` + `plugins/` layout, generated `.claude-plugin/marketplace.json`), how to add a new plugin, and how to validate changes before opening a PR (`mise exec -- scripts/validate.sh`).
