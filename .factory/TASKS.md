# Tasks

- [x] 1. Add a 3-line poem to `README.md` in a lightweight spot near the introduction, keeping the existing marketplace and install sections unchanged.
- [ ] 2. Spot-check the updated markdown so the poem renders as exactly 3 lines and the README still reads cleanly.
- [ ] 3. Confirm the change stays documentation-only with no edits to `apm.yml`, `plugins/factory/apm.yml`, or `.claude-plugin/marketplace.json`.
